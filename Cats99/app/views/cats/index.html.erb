<div>
    <h2>Cats index</h2>
</div>
<!-- er, pe, pc -->
<ul>
    <% @cats.each do |cat| %>
        <li>
            <a href="<%= cat_url(cat) %>">
                <%= cat.name %>
            </a>
        </li>
    <% end %>
</ul>